---
title: Transforms Catalog
description: Complete table of all built-in prompt transforms with descriptions.
---

All built-in transforms available via `prompt_transform()` and used by `prompt_probe` and `grid_search`.

## Outline

- Full table: transform name, description, example output
- Categories:
  - Abstraction shifts (abstract_up, concretize_down, etc.)
  - Perspective changes (first_person, expert_panel, etc.)
  - Dimensional manipulation (temporal_shift, scale_shift, etc.)
  - Inversion and contrast (inverse, devil_advocate, etc.)
  - Structural transforms (listicle, socratic, etc.)
  - Language-based transforms
- How to use each transform with `prompt_transform()`
- Which transforms work best for different use cases
